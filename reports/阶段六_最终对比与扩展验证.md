# 阶段六：参赛与论文双目标最终验证

## 阶段状态

进行中：冻结 Z0 的未来时间窗口标签与零样本评测已经完成，转入 W2 Z0 正交消融。阶段五 G 线
已经按预注册门停止，S5 与 P0/P1/P2/P3 均不启动。

## 阶段目标

以同一套证据同时完成两项目标：

1. **参赛目标**：形成面向 AIC“AI+学科交叉”的可复现算法作品、完整实验报告、静态解释图表
   和答辩材料，不开发独立交互平台。
2. **论文目标**：形成一篇定位务实的本科研究论文，重点证明确定性 OD 图传播、空间隔离评测、
   精确系统闭环和神经网络边界，不以顶刊顶会为首轮目标。

两条产出共用数据、代码、图表和结论，不为比赛与论文分别制造不同实验口径。

## 冻结研究定位

- **正式主方法**：Z0 无参数、无标签的双向固定需求传播。
- **神经网络结论**：G3 已解决深层训练数值稳定性并在三种子上改善平均 Spearman，但恒定学习率
  和调度版都未通过 NDCG@5 稳定性门，只作为“全局排序局部改善、头部增益不稳定”的边界证据。
- **最终应用问题**：在固定计算与索引预算下，平衡高需求区域效率与中长尾候选识别，并保持
  精确最短路答案不变。
- **禁止事项**：不启动 P 预训练，不继续扫描 G3 学习率、调度器、损失或残差尺度；不依据未来
  窗口、holdout 或最终在线结果回头选模型；不开发独立平台。

## 最终对比方法

- 原图双向 Dijkstra。
- 随机连通区域。
- OD 起终点热点区域。
- midpoint Proxy。
- Z0 确定性双向 OD 图传播。
- G3 `score = frozen_z0 + learned_residual`，只按已经冻结的 validation 结果报告，不再选型。
- 如增加使用路径信息的 oracle，必须单独标注为上界，不计入无路径监督主结果。

## 工作包与顺序

### W1：未来时间窗口验证（已完成）

完成下文已冻结的 Z0 未来窗口零样本验证。结果无论正负都直接报告，不修改窗口、查询样本、候选
或传播参数。

### W2：Z0 正交消融

在同一候选、切分和标签口径下至少比较：

- 仅起点需求与仅终点需求；
- 去除道路方向；
- 随机重连拓扑；
- 保持起终点边际需求的 OD 重耦合；
- 传播深度 `1/2/4/8/16/32` 与等权多深度读出；
- 随机、热点和 midpoint Proxy。

本项回答 Z0 的收益来自历史需求、真实拓扑、传播方向还是简单空间统计，禁止用单个最优消融反向
修改主方法。

实现入口为 `scripts/evaluate_z0_orthogonal_ablations.py`。固定 14 个配置：Z0、起点单侧、终点
单侧、无向图、度保持重连、边际保测度 OD 重耦合、六个单深度以及 mean/max 两个区域池化对照。
所有配置同时报告当前窗口与冻结未来窗口，但只作为解释性控制，不触发主方法替换。正式实验要求
CUDA；本机 75 项小图与结构测试已经通过，本机结果不作为正式实验结论。

### W3：非重叠集合选择

固定候选打分和总预算，比较：

- 直接 Top-K；
- Jaccard 重叠惩罚；
- 按新增唯一节点或成员的贪心边际覆盖。

统一报告 `K=5/10/18` 的真实收益、唯一节点覆盖、成员冗余、实际索引规模和边际收益。选择规则只
在 validation 上冻结，holdout 与未来窗口只评一次。

### W4：同预算精确系统闭环

对随机、热点、Proxy、Z0 和冻结 G3 使用相同索引预算，统一报告：

- 100% 距离正确率与不可达一致性；
- 平均、P50、P95 查询时间；
- 平均展开节点数与单查询加速比例；
- 预处理时间、训练/推理时间和索引存储；
- 热点、低需求、中长尾候选及端点局部接入的分组结果；
- 多个预算点的收益曲线和边际回报。

只有端到端查询收益才能支撑“计算预算优化”；Spearman 和 NDCG 只作为区域排序诊断。

### W5：参赛材料

不开发独立平台，直接复用研究产物形成：

- 交通科研需求与成本分析；
- 算法流程图、伪代码和复杂度；
- 静态地图、Top-K 表格、典型区域案例和预算曲线；
- 成功结果、负结果、适用边界与推广条件；
- 统一口径的申报书、汇报文稿和答辩材料。

若赛事要求视频，使用命令行运行记录、已有静态图表和讲解完成，不增加界面开发任务。

### W6：论文整理与增强

论文与比赛共用 W1～W4 的冻结结果，额外完成：

- 正式问题定义、方法伪代码、复杂度和无泄漏说明；
- 强基线、重复种子、统计波动、负结果与局限性；
- 与 workload-aware shortest-path 相关工作的准确差异；
- 完整复现实验配置和产物索引；
- 在时间与数据许可时补一个独立城市。第二城市不是参赛前置条件；若论文声称跨城市泛化，则必须
  在投稿前完成，否则只主张 Porto 数据上的方法与机制结论。

首轮投稿以方向匹配、正常同行评审的国内会议、专题研讨会或应用型期刊为目标，不以顶刊顶会为
完成条件，也不向异常收费或缺少正规评审的渠道投稿。投稿前争取交通工程或计算机方向教师进行
学术审核，署名严格按实际贡献确定。

## 必做实验摘要

1. **同预算主实验**：统一 shortcut 数、索引字节数或阶段三确定的主预算。
2. **消融实验**：移除起点场、终点场、道路方向、可学习传播、需求场交汇项或深度融合。
3. **稳定性实验**：多个随机种子和不同历史/未来 OD 划分。
4. **分组分析**：热点、冷门、纯压缩图查询与需要端点局部接入的查询分别统计。
5. **预算曲线**：比较不同预算下的收益与边际回报。
6. **集合选择**：在相同预算下比较直接 Top-K 与非重叠选择。
7. **论文增强项**：独立城市验证；未完成时不得声称跨城市泛化。

## 第一项：Z0 未来时间窗口零样本验证

本项在读取未来标签前冻结如下协议，不依据结果修改窗口、查询样本或 Z0 参数：

- Z0 输入仍固定为最早历史窗口 `H=[0,0.35)` 的 34,328 条 OD；传播方向、深度
  `1/2/4/8/16/32`、等权读出和区域聚合完全复用既有 Z0。
- 当前标签窗口 `Y=[0.35,0.70)` 不进入 Z0；未来标签窗口固定为剩余时间段
  `F=[0.70,1.00)`，完整窗口约 29,425 条 OD。
- 从 F 内以随机种子 42 抽取 2,000 条查询，与当前正式标签数量和抽样规则一致。
- 复用同一 1,200 个固定随机候选、同一单区域精确收益定义；端点缓存关闭，所有距离正确率必须
  为 100%。候选、H、Y 和 F 不得因结果重新筛选。
- 主结果为全候选 Spearman、NDCG@5/10/18 和 Top-K 平均真实收益；同时报告重叠组隔离后的
  train/validation/holdout 三个候选子集，但这些名称只表示空间分组，不用于未来协议选型。
- 额外报告 Y 标签直接预测 F 标签的排序相关性和 Top-K 重叠，区分 Z0 失效与真实区域收益本身的
  时间漂移。
- 本项是冻结零样本验证：F 标签不用于重新定向、校准、训练或选择 Z0。完成后才允许另立“用
  `[0.35,0.70)` OD 更新需求场”的滚动更新实验，二者不得混写。

实现入口为 `scripts/evaluate_z0_future_window.py`；标签继续由
`scripts/build_region_training_set.py` 的可恢复精确评测流程生成。

### 完成结果

未来标签于服务器完成并同步回本机：manifest 为 `status=complete`，候选数 `1,200/1,200`，
每个候选固定使用 seed 42 抽取的 2,000 条未来查询；全部候选 `correctness_rate=1.0`。本机重新
运行评测脚本后，`summary.json`、`predictions.csv` 和报告的 SHA-256 与服务器产物完全一致。

| 指标 | 当前 Y 全候选 | 未来 F 全候选 | 未来 F holdout |
| --- | ---: | ---: | ---: |
| Spearman | 0.935398 | 0.944656 | 0.938784 |
| NDCG@5 | 0.951078 | 0.944421 | 0.938935 |
| NDCG@10 | 0.938944 | 0.935608 | 0.972783 |
| NDCG@18 | 0.936265 | 0.933698 | 0.978020 |
| Top-5 平均真实收益 | 171.880 | 174.847 | 169.753 |
| Top-10 平均真实收益 | 166.775 | 170.839 | 160.454 |
| Top-18 平均真实收益 | 164.170 | 167.445 | 143.460 |

当前 Y 标签直接预测未来 F 标签的 Spearman 为 `0.995869`，平均标签收益仅变化 `-1.0303`，
绝对变化的 P95 为 `6.2613`。因此冻结 Z0 在本次未来窗口没有时间外崩溃，且全候选 Spearman
较当前窗口增加 `0.009258`；但主要原因之一是候选真实收益排序本身高度稳定。本项只能证明 Porto
当前稳定需求时段下的零样本泛化，不能声称模型已应对强分布漂移。

未来窗口中，Z0 与未来 oracle 的 Top-K 交集仅为 `1/5、3/10、7/18`，而当前与未来 oracle 的
交集为 `3/5、8/10、17/18`。高 Spearman/NDCG 与有限的严格 Top-K 集合重合并存，进一步说明
下一步必须评估非重叠集合选择和真实在线收益，不能仅凭全局相关性结束研究。

## 统一报告指标

- 平均、P50 和 P95 在线查询耗时。
- 平均展开节点数和单查询加速比例。
- 正确率与不可达查询一致性。
- 实际区域数、内部节点数、shortcut 数和压缩图规模。
- 端点局部接入查询数量、局部工作量和缓存命中率。
- 索引预处理时间、模型训练时间和推理时间。
- 多随机种子的均值、标准差和最差结果。

## 分目标完成标准

### 参赛完成

- W1～W4 的冻结实验完成并统一进入报告；
- 全部在线主结果保持 100% 距离正确率；
- 形成需求、方法、创新、效果、成本、边界和推广条件完整的申报与答辩材料；
- 静态地图与典型案例足以解释选区，不以平台界面作为完成标准。

### 论文可投稿

- 全部方法在测试集上保持 100% 距离正确率。
- 所有主表均为同预算比较。
- Z0 的来源有方向、拓扑、OD 和深度消融支持。
- G3 的正向与负向结论均有多种子和预注册门支持，不夸大监督学习收益。
- 至少完成冻结未来时间窗口验证；第二城市未完成时缩小论文主张，不宣称普适迁移。
- 最终结论同时报告正向结果、无显著差异和失败配置。

## 停止与范围控制

- 不因 G3 头部指标未通过而重启神经网络调参。
- 不为比赛开发独立交互平台。
- W1～W4 未闭环前，不提前扩展跨城市或新模型。
- 若未来窗口结果下降，先分析标签时间漂移，不回改冻结协议。
- 若非重叠选择不能提高真实收益，如实报告冗余与收益之间的权衡。
- “低需求/中长尾区域支持”必须由分组指标支撑；未完成空间距离分析前不写成“偏远地区支持”。

## 当前风险

- 当前只有 Porto 数据，第二城市及其真实 OD 来源尚未确定。
- Z0 已在本次稳定未来窗口保持强排序，但当前/未来标签 Spearman 高达 `0.9959`，强需求漂移下的
  泛化仍未验证。
- midpoint Proxy 与 Z0 在部分固定 K 上可能选择相同候选；必须依靠全局排序、小 K、非重叠集合
  和真实在线收益区分方法价值，不能只报告单个 K。
- G3 已确认不能稳定改善头部排序，不得把平均 Spearman 小幅提升写成全面超过 Z0。
- 正确道路拓扑、传播方向和 OD 耦合的独立贡献仍需 Z0 同口径正交消融确认。
- Top-K 候选重叠明显；非重叠集合选择能否把排序改善转化为真实系统收益仍未知。
- 若不同策略实际索引规模差异较大，时间对比会失去公平性。
