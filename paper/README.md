# 论文初稿

本目录是独立论文工作区，当前正文为中文 LaTeX 初稿，按照英文期刊论文结构组织。确定目标期刊
后再整体翻译并替换为期刊模板，避免现在过早绑定版式。

## 当前文件

- `main.tex`：完整初稿正文，包含摘要、引言、相关工作、问题定义、方法、实验、结果、讨论、
  局限性和结论；
- `references.bib`：正文实际引用的参考文献；
- `EVIDENCE.md`：正文数字到仓库冻结报告和机器可读产物的对应关系；
- `figures/README.md`：正式制图清单和每张图的数据来源；
- `figures/`：后续只存论文正式图，不把训练日志或中间产物复制进来。

作者顺序已经按当前确认信息填写：

1. 席圣洋，第一作者；
2. 肖逸涵，第二作者；
3. 王加文，第二作者。

作者单位、英文姓名、邮箱、通讯作者、基金、利益冲突、作者贡献和致谢均未推测，正文中保持空白。

## 编译

已在本机使用 XeLaTeX 与 BibTeX 成功生成 `main.pdf`。当前版本为 A4 纸、14 页。
需要重新生成时，可在本目录运行：

```bash
xelatex main.tex
bibtex main
xelatex main.tex
xelatex main.tex
```

或者：

```bash
latexmk -xelatex -bibtex main.tex
```

正文采用 `ctexart`，必须使用 XeLaTeX 或 LuaLaTeX，不建议使用 pdfLaTeX。

## 冻结口径

- 底层索引明确写成已有 CRP/Graph Separator 式边界覆盖图，不主张原创压缩结构；
- 论文差异化核心是选择性区域物化、精确区域收益、无历史路径输入、时空隔离和严格不重叠部署；
- Z0 是面向本任务的无参数双向多尺度需求传播组合，不主张新的基础扩散算子；
- G4 是受约束的神经排序残差与实证贡献，不主张新的通用 GNN、残差结构或排序损失；
- G4 稳定改善全局 Spearman，但不写成全面改善 Top-K 或在线收益；
- 全部表格数字来自 `reports/最终研究成果与论文结论.md` 的冻结结果。

## 仍需人工确认

- 三位作者的英文姓名拼写；
- 作者单位及规范英文名称；
- 是否存在并列第二作者标注要求；
- 通讯作者、邮箱和作者贡献；
- 基金、利益冲突、数据/代码公开链接；
- 目标期刊及其篇幅、摘要、图表和参考文献格式要求。
