# Porto 第二版候选区域空间可视化

- 候选数量：1,200；每个候选 512 个节点。
- 路网节点覆盖：130,604，占全部节点 97.58%。
- 被覆盖节点平均出现在 4.70 个候选中，最大为 18 个。
- 候选包围盒对角线中位数：1.828 km；P10–P90 为 1.356–2.726 km。
- 候选凸包等效直径中位数：1.160 km；P10–P90 为 0.842–1.562 km。

图中 A 面板叠加全部候选凸包和中心，B 面板显示每个节点被多少候选覆盖，底部三个面板分别展示凸包面积约位于 P10、P50 和 P90 的固定随机候选。
